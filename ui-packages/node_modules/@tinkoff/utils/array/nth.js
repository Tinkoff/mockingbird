"use strict";

var curryN = require('../function/curryN');

/**
 * Returns the nth element of the given array. If n is negative the
 * element at index length + n is returned.
 *
 * @param {Number} index
 * @param {*} arr
 * @return {*}
 * @example
 *
 *      var list = ['foo', 'bar', 'baz', 'quux'];
 *      nth(1, list); //=> 'bar'
 *      nth(2, list); //=> 'baz'
 *      nth(-1, list); //=> 'quux'
 *      nth(-99, list); //=> undefined
 */
module.exports = curryN(2, function (index, arr) {
  if (index === void 0) {
    index = 0;
  }

  if (arr === void 0) {
    arr = [];
  }

  if (index < 0) {
    index += arr.length;
  }

  return arr[index];
});