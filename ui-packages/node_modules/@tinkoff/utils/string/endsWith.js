"use strict";

var curryN = require('../function/curryN');

/**
 * Checks if a string ends with the provided postfix
 *
 * @param {string} postfix
 * @param {string} str
 * @return {Boolean}
 * @example
 *
 *      endsWith('c', 'abc')                //=> true
 *      endsWith('b', 'abc')                //=> false
 */
module.exports = curryN(2, function (postfix, str) {
  if (postfix === void 0) {
    postfix = '';
  }

  if (str === void 0) {
    str = '';
  }

  return str.slice(-postfix.length) === postfix;
});