"use strict";

var curryN = require('../function/curryN');

var isString = require('../is/string');

/**
 * Returns a string containing a repeated identical value.
 *
 * @param {Number} n The number to repeat value.
 * @param {String} value The value to repeat.
 * @return {String} A new string containing `n` `value`s.
 * @example
 *
 *      repeat(5, 'h'); //=> 'hhhhh'
 *
 */
module.exports = curryN(2, function (n, value) {
  if (n === void 0) {
    n = 0;
  }

  if (!isString(value)) {
    return '';
  }

  var result = '';

  for (var i = 0; i < n; i++) {
    result += value;
  }

  return result;
});