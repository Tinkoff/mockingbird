"use strict";

var curryN = require('../function/curryN');

/**
 * Runs the given promise with the supplied argument. Returns the argument, when promise is resolved.
 *
 * @param {Function} fn The function to call with `x`. The return value of `fn` will be thrown away.
 * @param {*} x
 * @return {*} `x`.
 * @example
 *
 *      var sayX = x => console.log('x is ' + x);
 *      Promise.resolve(5).then(promiseTap(sayX));
 *      // logs 'x is 5'
 */
module.exports = curryN(2, function (fn, x) {
  return Promise.resolve().then(function () {
    return fn(x);
  }).then(function () {
    return x;
  });
});