/**
 * Checks if `test` is finite.
 *
 * @param {*} test The value to check.
 * @returns {boolean} Returns `true` if `test` is finite, else `false`.
 * @example
 *
 * isFinite(5); // => true
 * isFinite(null); // => false
 */
declare const _default: (test: any) => test is number;
export default _default;
