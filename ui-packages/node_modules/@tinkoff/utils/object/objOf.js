"use strict";

var curryN = require('../function/curryN');

/**
 * Creates an object containing a single key:value pair.
 *
 * @param {String} key
 * @param {*} value
 * @return {Object}
 * @example
 *
 *      objOf('key', 5) // => { key: 5 }
 */
module.exports = curryN(2, function (key, value) {
  var _ref;

  return _ref = {}, _ref[key] = value, _ref;
});