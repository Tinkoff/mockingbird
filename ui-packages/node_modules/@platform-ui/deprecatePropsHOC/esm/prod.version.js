import React from 'react';
export const deprecateProps = () => {
  return WrappedComponent => {
    const withDeprecatedProps = props => /*#__PURE__*/React.createElement(WrappedComponent, props);

    withDeprecatedProps.displayName = WrappedComponent.displayName || WrappedComponent.name || 'Component(!)';
    process.env.NODE_ENV !== "production" ? withDeprecatedProps.propTypes = WrappedComponent.propTypes : void 0;
    withDeprecatedProps.defaultProps = WrappedComponent.defaultProps;
    return withDeprecatedProps;
  };
};