import { getIconClass } from '@platform-ui/icon';
import icon from './Document.svg';
export default getIconClass(icon, {
  iconSize: 24,
  theme: {
    color: 'rgba(224, 31, 25, 1)',
    background: 'rgba(224, 31, 25, .15)'
  }
});