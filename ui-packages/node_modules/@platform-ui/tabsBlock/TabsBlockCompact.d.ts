import { PureComponent } from 'react';
import type { Size, Item, OnItemClick } from './types';
export declare type TabsBlockCompactProps = {
    /**
     * Размер элементов в группе
     */
    size: Size;
    /**
     * Индекс активного элемента
     */
    activeIndex: number;
    /**
     * Массив элементов меню
     */
    items: Item[];
    /**
     * Обработчик события onChange
     */
    onItemClick?: OnItemClick;
    /**
     * Флаг, делающий все элементы disabled
     */
    disabled?: boolean;
    /**
     * Индекс элемента
     */
    index?: number;
};
declare class TabsBlockCompact extends PureComponent<TabsBlockCompactProps> {
    static defaultProps: {
        size: string;
        activeIndex: number;
        items: never[];
    };
    render(): JSX.Element;
}
export default TabsBlockCompact;
