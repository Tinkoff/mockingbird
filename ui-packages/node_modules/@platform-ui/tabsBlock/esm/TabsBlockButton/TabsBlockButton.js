import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
const _excluded = ["size", "checked", "wide", "first", "last", "beforeActive", "afterActive"];
import React from 'react';
import cn from 'classnames';
import Clickable from '@platform-ui/clickable';
import { useFlexItemProps } from '@platform-ui/styleProps';
import styles from './TabsBlockButton.module.css';

const TabsBlockButton = _ref => {
  let {
    size,
    checked,
    wide,
    first,
    last,
    beforeActive,
    afterActive
  } = _ref,
      props = _objectWithoutPropertiesLoose(_ref, _excluded);

  const propsWithStyle = useFlexItemProps(props);
  return /*#__PURE__*/React.createElement(Clickable, Object.assign({}, propsWithStyle, {
    className: cn({
      [styles.root]: true,
      [styles.root_wide]: wide,
      [styles.root_first]: first,
      [styles.root_last]: last,
      [styles.root_beforeActive]: beforeActive,
      [styles.root_afterActive]: afterActive,
      [styles.root_checked]: checked,
      [styles.root_disabled]: props.disabled,
      [styles[`root_size_${size}`]]: size
    }),
    innerClassName: styles.inner
  }));
};

export default TabsBlockButton;