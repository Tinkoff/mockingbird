import React from 'react';
import type { FlexItemProps } from '@platform-ui/styleProps';
import type { Size } from '../types';
export interface TabsBlockButtonProps extends React.HTMLAttributes<HTMLElement>, FlexItemProps {
    size: Size;
    checked?: boolean;
    disabled?: boolean;
    wide?: boolean;
    first?: boolean;
    last?: boolean;
    beforeActive?: boolean;
    afterActive?: boolean;
    children?: React.ReactNode;
}
declare const TabsBlockButton: ({ size, checked, wide, first, last, beforeActive, afterActive, ...props }: TabsBlockButtonProps) => JSX.Element;
export default TabsBlockButton;
