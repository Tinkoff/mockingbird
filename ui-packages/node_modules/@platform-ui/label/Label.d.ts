import React from 'react';
import { DataQaInterface } from '@platform-ui/dataQa';
export interface LabelProps extends DataQaInterface {
    /**
     * Размер
     *
     * `"l"` – шрифт 16px, высота 24px
     *
     * `"s"` – шрифт 13px, высота 16px
     * @default "l"
     */
    size?: 's' | 'l';
    /**
     * Инпут
     */
    input: React.ReactNode;
    /**
     * Текст лейбла
     */
    children?: React.ReactNode;
    disabled?: boolean;
}
declare const Label: (props: LabelProps) => JSX.Element;
export default Label;
