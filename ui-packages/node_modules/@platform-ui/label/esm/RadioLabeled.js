import { RadioStateless } from '@platform-ui/radio';
import { withCheckableInputState } from '@platform-ui/stateUtils';
import { withLabel } from './withLabel';
export const RadioLabeledStateless = withLabel(RadioStateless, {
  dataQaType: 'uikit/radioLabeled'
});
export const RadioLabeled = withCheckableInputState(RadioLabeledStateless);