"use strict";

exports.__esModule = true;
exports.useFocusedState = void 0;

var _react = require("react");

var useFocusedState = function useFocusedState(props) {
  var focused = props.focused,
      onFocus = props.onFocus,
      onBlur = props.onBlur;

  var _useState = (0, _react.useState)(false),
      stateFocused = _useState[0],
      setStateFocused = _useState[1];

  return {
    focused: focused !== undefined ? focused : stateFocused,
    onFocus: (0, _react.useCallback)(function (event, params) {
      setStateFocused(true);
      onFocus && onFocus(event, params);
    }, [onFocus]),
    onBlur: (0, _react.useCallback)(function (event, params) {
      setStateFocused(false);
      onBlur && onBlur(event, params);
    }, [onBlur])
  };
};

exports.useFocusedState = useFocusedState;