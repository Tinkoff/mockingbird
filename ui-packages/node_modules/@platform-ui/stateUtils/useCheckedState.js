"use strict";

exports.__esModule = true;
exports.useCheckedState = void 0;

var _react = require("react");

var useCheckedState = function useCheckedState(props) {
  var checked = props.checked,
      defaultChecked = props.defaultChecked,
      onChange = props.onChange;

  var _useState = (0, _react.useState)(defaultChecked || false),
      stateChecked = _useState[0],
      setStateChecked = _useState[1];

  return {
    checked: checked !== undefined ? checked : stateChecked,
    onChange: (0, _react.useCallback)(function (event, params) {
      setStateChecked(params.checked);
      onChange && onChange(event, params);
    }, [onChange])
  };
};

exports.useCheckedState = useCheckedState;