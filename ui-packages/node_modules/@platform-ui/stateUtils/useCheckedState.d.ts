import type { EventHandler } from '@platform-ui/types';
export declare const useCheckedState: <TEvent, TParams extends {
    checked: boolean;
}>(props: {
    defaultChecked?: boolean | undefined;
    checked?: boolean | undefined;
    onChange?: EventHandler<TEvent, TParams> | undefined;
}) => {
    checked: boolean;
    onChange: EventHandler<TEvent, TParams>;
};
