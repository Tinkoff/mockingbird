import React from 'react';
import type { DataQaInterface } from '@platform-ui/dataQa';
export interface InlinePlaceholderProps extends DataQaInterface {
    /**
     * Включает частичный плейсхолдер у значений с типом  string
     */
    partialPlaceholder?: boolean;
    /**
     * Значение поля
     */
    value?: string;
    /**
     * Минимальная ширина контейнера с полем
     */
    minWidth?: React.CSSProperties['minWidth'];
    /**
     * Значение перед полем
     */
    prefix?: React.ReactNode;
    /**
     * Текст плейсхолдера
     */
    placeholder?: React.ReactNode;
    /**
     * Поле ввода
     */
    input?: React.ReactNode;
    /**
     * Временный проп для переопределения стилей, не предназначен для широкого использования.
     * Создан специально для задачи https://
     * TODO: удалить когда появится общий механизм темизации.
     */
    __overrideStyles?: {
        InlinePlaceholder?: {
            [key: string]: string;
        };
        [key: string]: any;
    };
    /**
     * Включает возможность редактировать свойство whiteSpace
     */
    whiteSpace?: React.CSSProperties['whiteSpace'];
}
declare const InlinePlaceholder: {
    ({ input, value, placeholder, partialPlaceholder, minWidth, dataQaType, prefix, __overrideStyles, whiteSpace }: InlinePlaceholderProps): JSX.Element;
    defaultProps: {
        dataQaType: string;
    };
};
export default InlinePlaceholder;
