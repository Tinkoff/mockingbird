import type { FormatProps } from './types';
export declare function addStep(value: number, props: FormatProps, count?: number): number;
export declare function canAddStep(value: number, props: FormatProps, count?: number): boolean;
