import React from 'react';
import type { DataQaInterface } from '@platform-ui/dataQa';
export declare type AutoComplete = string | boolean;
export declare type InputRef = HTMLInputElement;
interface WithCustomAutoComplete {
    /**
     * Включает autoComplete у нативномого инпута
     */
    autoComplete?: AutoComplete;
}
export interface InlineInputProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'autoComplete' | 'prefix'>, Pick<WithCustomAutoComplete, 'autoComplete'>, DataQaInterface {
    /**
     * Значение поля
     */
    value?: string;
    /**
     * Минимальная ширина контейнера с полем
     */
    minWidth?: React.CSSProperties['minWidth'];
    /**
     * Включает необходимые атрибуты для кобразуинга
     */
    cobrowsingSecure?: boolean;
    ellipsis?: boolean;
    refCallback?: React.Ref<HTMLInputElement>;
}
declare const InlineInput: React.ForwardRefExoticComponent<InlineInputProps & React.RefAttributes<HTMLInputElement>>;
export default InlineInput;
