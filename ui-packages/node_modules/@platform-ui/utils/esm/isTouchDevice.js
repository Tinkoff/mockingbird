const isTouchDevice = typeof window !== 'undefined' && ('ontouchstart' in window || window.DocumentTouch && document instanceof window.DocumentTouch);
/**
 * @deprecated
 * Используйте isTouchDevice из @platform-ui/device-info
 */

export default isTouchDevice;