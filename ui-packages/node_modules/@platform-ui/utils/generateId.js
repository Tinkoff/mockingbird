"use strict";

exports.__esModule = true;
exports.default = void 0;
var memo = {};

var getName = function getName(Component) {
  return Component && Component.constructor && Component.constructor.name || 'TCSid';
};

var Generator = function Generator(name) {
  var _this = this;

  this.createId = function () {
    return _this.name + _this.counter++;
  };

  this.name = name;
  this.counter = 1;
};

var generateId = function generateId(Component) {
  var componentName = getName(Component);
  var generator = memo[componentName];

  if (!generator) {
    generator = new Generator(componentName);
    memo[componentName] = generator;
  }

  return generator.createId();
};

var _default = generateId;
exports.default = _default;