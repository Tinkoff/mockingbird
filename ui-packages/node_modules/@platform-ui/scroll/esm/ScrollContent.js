import React, { forwardRef } from 'react';
import dataQa from '@platform-ui/dataQa';
import { useScrollbarWidth } from '@platform-ui/dom-utils';
import cn from 'classnames';
import styles from './Scroll.module.css';
import { windowStyles } from './utils';
export const ScrollContent = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    dataQaType = 'uikit/scroll',
    scrollY,
    scrollX,
    onScroll,
    onWheel,
    id,
    children
  } = props;
  const width = useScrollbarWidth();
  return /*#__PURE__*/React.createElement("div", Object.assign({}, dataQa({
    dataQaType: `${dataQaType}.content`
  }), {
    ref: ref,
    className: cn({
      [styles.content]: true,
      [styles.content_scrollY]: scrollY,
      [styles.content_scrollX]: scrollX
    }),
    id: id,
    style: windowStyles(scrollY, scrollX, width),
    onScroll: onScroll,
    onWheel: onWheel
  }), children);
});