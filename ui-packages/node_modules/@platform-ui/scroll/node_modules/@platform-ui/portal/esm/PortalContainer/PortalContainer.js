import React from 'react';
import PortalContext from '../PortalContext';
import { PortalWrapper } from '../PortalWrapper';
import s from './PortalContainer.module.css';

/**
 * @deprecated используйте `PortalWrapper`
 */
const PortalContainer = props => {
  const {
    children
  } = props;
  const portalExitRef = React.useRef(null);
  const contextValue = React.useMemo(() => ({
    getPortalContainer: () => portalExitRef.current
  }), []);
  return /*#__PURE__*/React.createElement(PortalContext.Provider, {
    value: contextValue
  }, /*#__PURE__*/React.createElement(PortalWrapper, {
    className: s.container,
    exitRef: portalExitRef
  }, children));
};

export default PortalContainer;