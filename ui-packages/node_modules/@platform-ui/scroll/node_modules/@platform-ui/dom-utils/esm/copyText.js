export function copyText(str) {
  const element = document.createElement('textarea');
  element.value = str; // eslint-disable-next-line compat/compat

  document.body.appendChild(element);
  element == null ? void 0 : element.select();
  document.execCommand('copy'); // eslint-disable-next-line compat/compat

  document.body.removeChild(element);
}