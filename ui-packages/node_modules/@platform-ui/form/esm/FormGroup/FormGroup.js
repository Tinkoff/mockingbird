import _extends from "@babel/runtime/helpers/extends";
import React, { Children, isValidElement } from 'react';
import dataQa from '@platform-ui/dataQa';
import cn from 'classnames';
import styles from './FormGroup.module.css';

const FormGroup = props => {
  const {
    children,
    grows,
    rowOnPhone,
    dataQaType = 'uikit/formGroup',
    elementStyles = []
  } = props;
  return /*#__PURE__*/React.createElement("div", Object.assign({}, dataQa({
    dataQaType
  }), {
    className: cn(styles.root, {
      [styles.root_rowOnPhone]: rowOnPhone
    })
  }), Children.map(children, (item, index) => {
    return /*#__PURE__*/isValidElement(item) ? /*#__PURE__*/React.createElement("div", Object.assign({}, dataQa({
      dataQaType: `${dataQaType}.element`
    }), {
      className: styles.element,
      style: _extends({
        flexGrow: grows && grows[index]
      }, elementStyles[index] ? elementStyles[index] : {})
    }), item) : null;
  }));
};

export default FormGroup;