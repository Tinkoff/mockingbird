import React from 'react';
declare type UrlProp = {
    url: string;
};
declare type IdProp = {
    id: string;
};
declare type IconProps = Partial<UrlProp & IdProp>;
export declare type SVGIconProps = React.HTMLAttributes<SVGElement> & {
    icon: string | (IconProps & UrlProp) | (IconProps & IdProp);
};
declare const SVGIcon: ({ icon, ...props }: SVGIconProps) => JSX.Element;
export default SVGIcon;
