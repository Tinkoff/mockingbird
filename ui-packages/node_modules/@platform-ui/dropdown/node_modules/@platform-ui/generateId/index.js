"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;

exports.__esModule = true;
exports.default = void 0;
exports.useId = useId;

var _react = _interopRequireDefault(require("react"));

var _nonSecure = require("nanoid/non-secure");

var getName = function getName(Component) {
  return Component && Component.constructor && Component.constructor.name || 'dsId';
};

function generateId(Component) {
  var componentName = getName(Component);
  var randomId = (0, _nonSecure.nanoid)(10);
  return componentName + "_" + randomId;
}

function useId(id) {
  var idRef = _react.default.useRef();

  if (id) {
    return id;
  }

  if (!idRef.current) {
    idRef.current = generateId();
  }

  return idRef.current;
}

var _default = generateId;
exports.default = _default;