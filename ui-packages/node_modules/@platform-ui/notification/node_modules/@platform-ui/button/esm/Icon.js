import React from 'react';
const ICON_SIZE = 24;

const Icon = ({
  icon
}) => {
  const IconTag = icon;
  return /*#__PURE__*/React.createElement(React.Fragment, null, typeof icon === 'object' ? icon : /*#__PURE__*/React.createElement(IconTag, {
    iconSize: ICON_SIZE
  }));
};

export default Icon;