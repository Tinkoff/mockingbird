import React from 'react';
import dataQa from '@platform-ui/dataQa';
import BadgeBase from '@platform-ui/badge';
const MAX_DISPLAY_COUNT = 99;

const Badge = props => {
  const {
    count
  } = props;
  return /*#__PURE__*/React.createElement(BadgeBase, Object.assign({
    color: "yellow",
    size: "s",
    padding: 8
  }, dataQa(props)), count > MAX_DISPLAY_COUNT ? '99+' : count);
};

export default Badge;