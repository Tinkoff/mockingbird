import { FC } from 'react';
import { DataQaInterface } from '@platform-ui/dataQa';
export declare type ContainerProps = DataQaInterface & {
    fullWidth?: boolean;
    mobile?: boolean;
};
declare const Container: FC<ContainerProps>;
export default Container;
