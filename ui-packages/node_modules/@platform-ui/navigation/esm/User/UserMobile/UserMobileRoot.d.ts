import React from 'react';
export declare type UserMobileRootProps = {
    children?: React.ReactNode | (({ closeProfilePopover }: {
        closeProfilePopover: () => void;
    }) => React.ReactNode);
};
declare const UserMobileRoot: (props: UserMobileRootProps) => JSX.Element;
export default UserMobileRoot;
