import React from 'react';
import { LinkProps } from '../../Link';
export declare type LogoutProps = Omit<LinkProps, 'title'> & {
    title: React.ReactNode;
    mobile?: boolean;
};
declare const Logout: (props: LogoutProps) => JSX.Element;
export default Logout;
