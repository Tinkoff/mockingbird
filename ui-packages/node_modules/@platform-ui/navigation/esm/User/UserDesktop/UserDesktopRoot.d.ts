import React from 'react';
import { DataQaInterface } from '@platform-ui/dataQa';
export declare type UserDesktopRootProps = DataQaInterface & {
    menuComponent?: React.ReactNode;
    children: React.ReactNode | (({ closeProfilePopover }: {
        closeProfilePopover: () => void;
    }) => React.ReactNode);
};
declare const UserDesktopRoot: (props: UserDesktopRootProps) => JSX.Element;
export default UserDesktopRoot;
