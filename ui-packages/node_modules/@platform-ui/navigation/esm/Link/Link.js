import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
const _excluded = ["external", "onClick", "path", "dataQaType", "dataQaData"];
import React, { forwardRef } from 'react';
import dataQa from '@platform-ui/dataQa';

const handleLinkClick = ({
  path,
  onClick
}) => {
  return e => {
    onClick && onClick(e, {
      path
    });
  };
};

const Link = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    path
  } = props,
        restProps = _objectWithoutPropertiesLoose(props, _excluded);

  const handleMouseDown = e => {
    e.preventDefault();
  };

  const handleClick = e => {
    return handleLinkClick(props)(e);
  };

  return /*#__PURE__*/React.createElement("a", Object.assign({
    ref: ref,
    href: path,
    onMouseDown: handleMouseDown,
    onClick: handleClick
  }, restProps, dataQa(props)));
});
export default Link;