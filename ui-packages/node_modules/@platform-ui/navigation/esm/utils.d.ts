import type { ContextMenuItem } from '@platform-ui/contextMenu';
import type { MenuItemProps } from './types';
export declare function menuItemToContextMenu({ active, path, title, onClick }: MenuItemProps, clickHandler?: MenuItemProps['onClick']): ContextMenuItem;
