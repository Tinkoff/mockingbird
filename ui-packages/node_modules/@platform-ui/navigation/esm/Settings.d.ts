import React from 'react';
import type { Theme } from './NavigationContext';
export interface SettingsProps {
    theme: Theme;
    children: React.ReactNode;
}
declare const Settings: (props: SettingsProps) => JSX.Element;
export default Settings;
