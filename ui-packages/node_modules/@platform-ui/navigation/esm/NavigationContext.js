import React from 'react';
const defaultValue = {
  theme: undefined
};
const NavigationContext = /*#__PURE__*/React.createContext(defaultValue);
export default NavigationContext;
export const useNavigationContext = () => React.useContext(NavigationContext);