"use strict";

exports.__esModule = true;
exports.menuItemToContextMenu = menuItemToContextMenu;

function menuItemToContextMenu(_ref, clickHandler) {
  var active = _ref.active,
      path = _ref.path,
      title = _ref.title,
      onClick = _ref.onClick;
  var handleClick = onClick || clickHandler;
  return {
    active: active,
    attributes: {
      href: path
    },
    tagName: path ? 'a' : 'span',
    title: title,
    onClick: function onClick(e) {
      if (handleClick) {
        handleClick(e, {
          path: path
        });
      }
    }
  };
}