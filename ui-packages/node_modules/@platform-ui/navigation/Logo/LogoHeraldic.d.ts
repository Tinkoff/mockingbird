/// <reference types="react" />
import type { StackProps } from '@platform-ui/layout';
import type { LogoProps } from './index';
export interface LogoHeraldicProps extends LogoProps, Partial<Pick<StackProps, 'direction' | 'gaps'>> {
    imgWidth?: number;
    imgHeight?: number;
}
declare const LogoHeraldic: (props: LogoHeraldicProps) => JSX.Element;
export default LogoHeraldic;
