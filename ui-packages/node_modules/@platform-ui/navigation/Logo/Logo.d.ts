/// <reference types="react" />
import type { DataQaInterface } from '@platform-ui/dataQa';
export interface LogoProps extends DataQaInterface {
    lang?: 'ru' | 'en';
}
declare const Logo: (props: LogoProps) => JSX.Element;
export default Logo;
