/// <reference types="react" />
import { DataQaInterface } from '@platform-ui/dataQa';
import type { MenuItemProps } from '../types';
import type { Theme } from '../NavigationContext';
declare type MoreButtonProps = DataQaInterface & {
    items: MenuItemProps[];
    theme?: Theme;
};
declare const MoreButton: (props: MoreButtonProps) => JSX.Element;
export default MoreButton;
