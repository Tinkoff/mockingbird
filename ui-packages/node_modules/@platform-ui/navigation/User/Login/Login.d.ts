import React from 'react';
import { LinkProps } from '../../Link';
export declare type LoginProps = Omit<LinkProps, 'title'> & {
    title: React.ReactNode;
    mobile?: boolean;
};
declare const Login: (props: LoginProps) => JSX.Element;
export default Login;
