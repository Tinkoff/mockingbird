import _extends from "@babel/runtime/helpers/extends";
export const createPropsPicker = keys => props => {
  const pickedProps = {};

  const restProps = _extends({}, props);

  keys.forEach(key => {
    if (Object.prototype.hasOwnProperty.call(restProps, key)) {
      pickedProps[key] = restProps[key];
      delete restProps[key];
    }
  });
  return [pickedProps, restProps];
};