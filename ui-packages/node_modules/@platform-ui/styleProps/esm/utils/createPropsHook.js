import _extends from "@babel/runtime/helpers/extends";
export const createPropsHook = (pickStyleProps, useStyle) => props => {
  const [styleProps, restProps] = pickStyleProps(props);
  const style = useStyle(styleProps);
  return _extends({}, restProps, {
    style: _extends({}, restProps.style || {}, style)
  });
};