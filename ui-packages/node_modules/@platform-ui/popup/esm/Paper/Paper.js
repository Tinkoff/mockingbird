import _extends from "@babel/runtime/helpers/extends";
import React from 'react';
import cn from 'classnames';
import dataQa from '@platform-ui/dataQa';
import { usePaddingStyle } from '@platform-ui/styleProps';
import styles from './Paper.module.css';

const Paper = props => {
  const {
    dataQaType = 'uikit/popup.paper',
    size = 'l',
    mobileSize = 's',
    media,
    disableHorizontalPadding,
    children,
    backgroundColor,
    alignCross = 'center',
    minWidth,
    gap,
    mobileGap
  } = props;
  const paddingStyle = usePaddingStyle(props);
  const customSize = typeof size === 'number';
  return /*#__PURE__*/React.createElement("div", Object.assign({
    className: cn(styles.root, {
      [styles[`root_size_${customSize ? 'custom' : size}`]]: size,
      [styles[`root_media_${media}`]]: media,
      [styles[`root_mobileSize_${mobileSize}`]]: mobileSize,
      [styles.root_disableHorizontalPadding]: disableHorizontalPadding,
      [styles[`root_alignCross_${alignCross}`]]: alignCross
    }),
    style: {
      padding: media === 'phone' ? mobileGap : gap
    }
  }, dataQa({
    dataQaType
  })), /*#__PURE__*/React.createElement("div", Object.assign({}, dataQa({
    dataQaType: `${dataQaType}.container`
  }), {
    className: styles.paper,
    style: _extends({}, paddingStyle, {
      maxWidth: customSize ? `${size}px` : undefined,
      backgroundColor,
      minWidth
    })
  }), /*#__PURE__*/React.createElement("div", {
    className: styles.paperInner
  }, children)));
};

export default Paper;