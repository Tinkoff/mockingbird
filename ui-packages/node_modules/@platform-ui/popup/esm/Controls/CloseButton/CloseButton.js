import React from 'react';
import cn from 'classnames';
import dataQa from '@platform-ui/dataQa';
import CloseIcon from '@platform-ui/iconsPack/interface/16/Close';
import styles from './CloseButton.module.css';

const CloseButton = props => {
  const {
    onClick,
    theme,
    media,
    dataQaType,
    ariaLabel = 'Закрыть попап'
  } = props;

  const handleMouseDown = event => {
    event.preventDefault();
  };

  return /*#__PURE__*/React.createElement("button", Object.assign({
    type: "button",
    className: cn(styles.root, {
      [styles[`root_theme_${theme}`]]: theme,
      [styles[`root_media_${media}`]]: media
    }),
    onClick: onClick,
    onMouseDown: handleMouseDown,
    "aria-label": ariaLabel
  }, dataQa({
    dataQaType: `${dataQaType}.closeButton`
  })), /*#__PURE__*/React.createElement(CloseIcon, null));
};

export default CloseButton;