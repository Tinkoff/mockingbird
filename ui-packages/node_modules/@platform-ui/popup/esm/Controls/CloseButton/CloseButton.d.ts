/// <reference types="react" />
import type { DataQaInterface } from '@platform-ui/dataQa';
import type { OverlayProps } from '../../Overlay';
import type { Media } from '../../types';
export declare type CloseButtonProps = DataQaInterface & {
    onClick: OverlayProps['onClose'];
    theme: 'onOverlay' | 'onPaper' | 'onDark' | 'onLight';
    media: Media;
    ariaLabel?: string;
};
declare const CloseButton: (props: CloseButtonProps) => JSX.Element;
export default CloseButton;
