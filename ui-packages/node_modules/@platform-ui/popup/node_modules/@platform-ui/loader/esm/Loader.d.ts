import React from 'react';
import type { DataQaInterface } from '@platform-ui/dataQa';
export declare const THEMES: {
    readonly yellow: any;
    readonly black: any;
    readonly current: "currentColor";
};
export interface LoaderProps extends DataQaInterface {
    /**
     * Цветовая тема
     */
    color?: keyof typeof THEMES | React.CSSProperties['color'];
    /**
     * Размер
     */
    size?: 'xs' | 's' | 'm' | 'l' | 'xl' | 'xxl';
    /**
     * Флаг, отвечающий за скрытие элемента
     */
    hidden?: boolean;
    /**
     * Флаг состояния перекрывания основного контента
     */
    overlay?: boolean;
    /**
     * Включает центрирование блока по горизонтали
     */
    centered?: boolean;
    /**
     * Центрует по верхнему краю `overlay`, задавая отступ
     * Требуется `overlay: true`.
     */
    alignTop?: number | string | null;
    /**
     * Флаг прозрачности `overlay`
     * `true` - `opacity: 80%`
     * `false` - `opacity: 100%`
     */
    semiTransparentBg?: boolean;
    /**
     * Настройки анимации появления и исчезновения компонента
     */
    fadeAnimation?: 'false' | 'true' | 'start' | 'end';
}
declare const Loader: {
    (props: LoaderProps): JSX.Element | null;
    defaultProps: {
        dataQaType: string;
        size: string;
        color: string;
        centered: boolean;
        alignTop: null;
        hidden: boolean;
        semiTransparentBg: boolean;
        fadeAnimation: string;
    };
};
export default Loader;
