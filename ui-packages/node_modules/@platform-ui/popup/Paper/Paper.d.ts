import React from 'react';
import type { DataQaInterface } from '@platform-ui/dataQa';
import type { PaddingProps } from '@platform-ui/styleProps';
import type { Media } from '../types';
declare type CustomSize = number;
export declare type PaperProps = DataQaInterface & PaddingProps & React.PropsWithChildren<{
    /**
     * Размеры Popup на tablet-s и выше
     * @default "l"
     */
    size?: 's' | 'm' | 'l' | 'fullscreen' | 'auto' | CustomSize;
    /**
     * Размеры Popup на mobile
     * @default "s"
     */
    mobileSize?: 's' | 'fullscreen';
    /**
     * Размер экрана
     */
    media: Media;
    /**
     * Отключает горизонтальные паддинги
     * Нужно для показа блоков с подложкой [UIKIT-4737]
     * @deprecated используйте paddingX={0}
     */
    disableHorizontalPadding?: boolean;
    /**
     * Цвет фона
     */
    backgroundColor?: string;
    /**
     * Позиционирует Popup по вертикали.
     * Значение 'start' может быть более удобным для контента, меняющего высоту.
     * @default "center"
     */
    alignCross?: 'start' | 'center';
    /**
     * Минимальная ширина контейнера
     */
    minWidth?: React.CSSProperties['minWidth'];
    /**
     * Кастомное значение padding на корневом элементе на tablet-s и выше
     */
    gap?: React.CSSProperties['padding'];
    /**
     * Кастомное значение padding на корневом элементе на mobile
     */
    mobileGap?: React.CSSProperties['padding'];
}>;
declare const Paper: (props: PaperProps) => JSX.Element;
export default Paper;
