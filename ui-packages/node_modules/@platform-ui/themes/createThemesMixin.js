"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault").default;

var _extends2 = _interopRequireDefault(require("@babel/runtime/helpers/extends"));

var _require = require('./themeStylesJSON'),
    themeStylesJSON = _require.themeStylesJSON;

var createThemeMixin = function createThemeMixin(themes, options) {
  if (options === void 0) {
    options = {};
  }

  return function (mixin) {
    var themeKey = mixin.params.split(/\s+/g)[1];

    if (themeKey) {
      var _themeStylesJSON;

      return themeStylesJSON((_themeStylesJSON = {}, _themeStylesJSON[themeKey] = themes[themeKey], _themeStylesJSON), (0, _extends2.default)({}, options, {
        defaultThemeKey: themeKey
      }));
    }

    return themeStylesJSON(themes, options);
  };
};

module.exports = {
  createThemeMixin: createThemeMixin
};