import React from 'react';
import type { DataQaInterface } from '@platform-ui/dataQa';
import type { LabelProps } from './Label';
export interface LabeledInputProps extends DataQaInterface {
    size?: 's' | 'l';
    disabled?: boolean;
    input?: any | never;
    children?: any | never;
}
export declare type WithLabelProps<TProps extends LabeledInputProps> = Omit<TProps, keyof LabeledInputProps> & Omit<LabelProps, 'input'> & {
    /**
     * Слот для вывода подсказки
     */
    hintBlock?: React.ReactNode;
};
export declare const withLabel: <TProps extends LabeledInputProps>(Component: React.ComponentType<TProps>, defaults?: {
    dataQaType?: string;
}) => (props: WithLabelProps<TProps>) => JSX.Element;
