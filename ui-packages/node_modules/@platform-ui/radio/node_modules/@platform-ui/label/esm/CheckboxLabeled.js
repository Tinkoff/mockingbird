import { CheckboxStateless } from '@platform-ui/checkbox';
import { withCheckableInputState } from '@platform-ui/stateUtils';
import { withLabel } from './withLabel';
export const CheckboxLabeledStateless = withLabel(CheckboxStateless, {
  dataQaType: 'uikit/checkboxLabeled'
});
export const CheckboxLabeled = withCheckableInputState(CheckboxLabeledStateless);