import { ToggleStateless } from '@platform-ui/toggle';
import { withCheckableInputState } from '@platform-ui/stateUtils';
import { withLabel } from './withLabel';
export const ToggleLabeledStateless = withLabel(ToggleStateless, {
  dataQaType: 'uikit/toggleLabeled'
});
export const ToggleLabeled = withCheckableInputState(ToggleLabeledStateless);