"use strict";

var primitives = require('./primitives');

var semantic = require('./semantic');

var support = require('./support');

var typography = require('./typography');

module.exports = Object.assign({}, primitives, semantic, support, typography);