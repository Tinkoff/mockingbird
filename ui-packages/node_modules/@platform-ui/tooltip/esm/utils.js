export function equalStyles(styleA, styleB) {
  const aKeys = Object.keys(styleA);
  const bKeys = Object.keys(styleB);

  if (aKeys.length !== bKeys.length) {
    return false;
  }

  for (const key of aKeys) {
    if (styleA[key] !== styleB[key]) {
      return false;
    }
  }

  return true;
}