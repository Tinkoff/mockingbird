import React from 'react';
import type { PopoverProps } from '@platform-ui/popover/utils/types';
import type { TooltipBubbleProps } from './TooltipBubble';
export interface TooltipProps extends TooltipBubbleProps, Omit<PopoverProps, 'popoverBlock' | 'onActiveToggle' | 'role' | 'ariaDescribedBy'> {
    /**
     * Контент всплывашки
     */
    popoverContent?: React.ReactNode;
    /**
     * Свойство point-events тултипа
     */
    pointerEvents?: React.CSSProperties['pointerEvents'];
}
declare const Tooltip: ({ inline, children, withCorner, theme, popoverContent, alignText, overflow, increasedTouchZone, customPadding, direction: directionProp, minArrowOffset, hideOnTouchMove, dataQaType, cornerStyle: propsCornerStyle, pointerEvents, ...otherProps }: TooltipProps) => JSX.Element;
export default Tooltip;
