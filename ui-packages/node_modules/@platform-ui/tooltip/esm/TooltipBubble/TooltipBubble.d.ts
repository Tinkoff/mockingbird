import React from 'react';
import type { DataQaInterface } from '@platform-ui/dataQa';
import type { Direction, CornerStyle } from '@platform-ui/popover/utils/types';
export declare const props: {
    theme: readonly ["dark", "light"];
    align: readonly ["left", "center", "right", "inherit", "justify"];
};
export declare type TooltipBubbleProps = DataQaInterface & React.PropsWithChildren<{
    id?: React.HTMLAttributes<HTMLDivElement>['id'];
    /**
     * Цветовая схема
     */
    theme?: typeof props.theme[number];
    /**
     * Выравнивание текста (работает только для многострочного текста)
     */
    alignText?: typeof props.align[number];
    /**
     * Убирает стандартную ширину и внутренние отступы всплывашки
     */
    inline?: boolean;
    /**
     * Отвечает за оторбажение уголка у всплывашки
     */
    withCorner?: boolean;
    /**
     * С какой стороны всплывает tooltip
     */
    direction?: Direction | Direction[];
    cornerStyle?: CornerStyle;
    /**
     * css-свойство overflow
     */
    overflow?: 'hidden' | 'visible';
}>;
declare const TooltipBubble: ({ alignText, children, cornerStyle, direction, inline, withCorner, theme, id, overflow, dataQaType, ...otherProps }: TooltipBubbleProps) => JSX.Element;
export default TooltipBubble;
