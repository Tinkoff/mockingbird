import React from 'react';
import dataQa from '@platform-ui/dataQa';
import styles from './Block.module.css';

const handleClick = event => {
  /**
   * [UIKIT-5228]
   * Если target находится в портале, то клик не всплывёт до лейбла и ничего делать не нужно
   * Если же он находится внутри currentTarget (т.е. BlockHintWrapper),
   * то в нём нет никаких интерактивных элементов и можно смело делать preventDefault
   */
  if (event.currentTarget.contains(event.target)) {
    event.preventDefault();
  }
};

export const BlockHintWrapper = /*#__PURE__*/React.memo(props => {
  const {
    dataQaType = 'uikit/block.blockHintWrapper',
    children
  } = props;
  return /*#__PURE__*/React.createElement("div", Object.assign({}, dataQa({
    dataQaType
  }), {
    className: styles.hintBlock,
    onClick: handleClick
  }), children);
});