import { withCheckableInputState } from '@platform-ui/stateUtils';
import { ToggleStateless } from '@platform-ui/toggle';
import { withBlock } from './withBlock';
export const ToggleBlockStateless = withBlock(ToggleStateless, {
  dataQaType: 'uikit/toggleBlock'
});
export const ToggleBlock = withCheckableInputState(ToggleBlockStateless);