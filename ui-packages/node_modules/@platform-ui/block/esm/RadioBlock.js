import { withCheckableInputState } from '@platform-ui/stateUtils';
import { RadioStateless } from '@platform-ui/radio';
import { withBlock } from './withBlock';
export const RadioBlockStateless = withBlock(RadioStateless, {
  dataQaType: 'uikit/radioBlock'
});
export const RadioBlock = withCheckableInputState(RadioBlockStateless);