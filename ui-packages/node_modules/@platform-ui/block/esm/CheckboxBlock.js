import { withCheckableInputState } from '@platform-ui/stateUtils';
import { CheckboxStateless } from '@platform-ui/checkbox';
import { withBlock } from './withBlock';
export const CheckboxBlockStateless = withBlock(CheckboxStateless, {
  dataQaType: 'uikit/checkboxBlock'
});
export const CheckboxBlock = withCheckableInputState(CheckboxBlockStateless);