import React from 'react';
import { CarouselProps } from '@platform-ui/carousel';
import { IslandProps } from '../types';
export declare type ControllableIslandPagerProps = IslandProps & Pick<CarouselProps, 'autoHeight'> & {
    formatPaginationText?: (currentPage: number, totalPages: number) => string;
    onChange: (index: number) => void;
    activeIndex: number;
};
declare const ControllableIslandPager: React.FC<ControllableIslandPagerProps>;
export default ControllableIslandPager;
