import React, { useContext } from 'react';
import cn from 'classnames';
import Clickable from '@platform-ui/clickable';
import sizeContext from '../Island.context';
import styles from './IslandFooterButton.module.css';

const IslandFooterButton = props => {
  const size = useContext(sizeContext);
  return /*#__PURE__*/React.createElement(Clickable, Object.assign({}, props, {
    className: cn(styles.button, {
      [styles[`button_size_${size}`]]: !!size,
      [styles.button_disabled]: props.disabled
    }),
    innerClassName: styles.inner
  }));
};

export default IslandFooterButton;