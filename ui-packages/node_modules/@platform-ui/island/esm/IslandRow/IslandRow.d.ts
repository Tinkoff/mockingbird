import React from 'react';
export declare type IslandRowProps = {
    /**
     * Иконка слева
     */
    icon?: React.ReactNode;
    /**
     * Основное содержимое
     */
    children?: React.ReactNode;
    /**
     * Содержимое справа
     */
    side?: React.ReactNode;
    /**
     * Возможность переноса контента справа на следующую строку
     */
    wrapSide?: boolean;
};
declare const IslandRow: (props: IslandRowProps) => JSX.Element;
export default IslandRow;
