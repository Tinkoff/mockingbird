import React from 'react';
import styles from '../Island.module.css';

const IslandContentIcon = props => {
  const {
    picture
  } = props;
  return picture ? /*#__PURE__*/React.createElement("figure", {
    className: styles.picture
  }, picture) : null;
};

export default IslandContentIcon;