import React, { useState } from 'react';
import ControllableIslandPager from './ControllableIslandPager';
import { DEFAULT_PROPS } from '../types';

const IslandPager = props => {
  const [activeIndex, setActiveIndex] = useState(0);

  const handleChange = index => {
    setActiveIndex(index);
  };

  return /*#__PURE__*/React.createElement(ControllableIslandPager, Object.assign({}, props, {
    activeIndex: activeIndex,
    onChange: handleChange
  }));
};

IslandPager.defaultProps = DEFAULT_PROPS;
export default IslandPager;