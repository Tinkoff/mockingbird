import React from 'react';
import cn from 'classnames';
import Clickable from '@platform-ui/clickable';
import IconArrow from '@platform-ui/iconsPack/interface/16/ArrowDown';
import styles from './IslandPager.module.css';

const IslandPagerButton = props => {
  const {
    direction,
    disabled,
    onClick
  } = props;
  return /*#__PURE__*/React.createElement(Clickable, {
    className: cn(styles.button, {
      [styles.button_disabled]: disabled
    }),
    innerClassName: styles.buttonInner,
    disabled: disabled,
    onClick: onClick
  }, /*#__PURE__*/React.createElement(IconArrow, {
    rotate: direction === 'left' ? 90 : 270
  }));
};

export default IslandPagerButton;