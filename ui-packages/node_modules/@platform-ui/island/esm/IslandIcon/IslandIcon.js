import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
const _excluded = ["withContainer"];
import React, { useContext } from 'react';
import Icon from '@platform-ui/icon';
import sizeContext from '../Island.context';

const IslandIcon = props => {
  const {
    withContainer = true
  } = props,
        restProps = _objectWithoutPropertiesLoose(props, _excluded);

  const size = useContext(sizeContext);
  return /*#__PURE__*/React.createElement(Icon, Object.assign({}, restProps, {
    withContainer: withContainer,
    containerSize: size === 'l' ? 64 : 48
  }));
};

export default IslandIcon;