import type { MoneyProps } from './Money';
export declare function formatMoney({ value, currency, precision, showSmalls, showPlus, showCurrencyWithNull, trimZeros, decimalSeparator }: MoneyProps): {
    number: number;
    sign: string;
    numerals: string[];
    smalls: string;
    currency: any;
};
