"use strict";

exports.__esModule = true;
exports.equalStyles = equalStyles;

function equalStyles(styleA, styleB) {
  var aKeys = Object.keys(styleA);
  var bKeys = Object.keys(styleB);

  if (aKeys.length !== bKeys.length) {
    return false;
  }

  for (var _i = 0, _aKeys = aKeys; _i < _aKeys.length; _i++) {
    var _key = _aKeys[_i];

    if (styleA[_key] !== styleB[_key]) {
      return false;
    }
  }

  return true;
}