/// <reference types="react" />
import type { TooltipProps } from '../Tooltip';
export interface HintProps extends Omit<TooltipProps, 'children'> {
    /**
     * Название темы иконки
     */
    hintColor?: 'greyLighten20' | 'greyLighten40' | 'blue' | 'opacityDark' | 'opacityLight';
    /**
     * Показываем хинт по фокусу.
     * Включенный проп добавляет хинту tabIndex=0 и отслеживание событий фокуса и блюра.
     */
    focusable?: boolean;
}
declare const Hint: ({ hintColor, focusable, ...otherProps }: HintProps) => JSX.Element;
export default Hint;
