import type React from 'react';

declare const inputHintHOC: <T extends { hintBlock?: React.ReactNode }>(
    WrappedComponent: React.ComponentType<T>
) => React.ComponentType<
    Omit<T, 'hintBlock'> & {
        hintContent?: React.ReactNode;
        isActiveHint?: boolean;
        isInactiveHint?: boolean;
        hintBubbleTheme?: string;
    }
>;

export default inputHintHOC;
