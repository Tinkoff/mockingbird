export default class Mask {
    tokens: string[];
    length: number;
    maskTransitions: Record<string, any>;
    editableIndexes: boolean[];
    fixedPrefix: string;
    constructor(mask?: string, options?: {
        maskTransitions?: Record<string, any>;
    });
    mask(value: string, caretSymbol?: string): string;
    unmask(value?: string): string;
    getHintHolder(): string;
}
