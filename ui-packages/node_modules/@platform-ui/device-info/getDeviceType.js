"use strict";

exports.__esModule = true;
exports.getDeviceType = getDeviceType;
var memo;

function calc() {
  var isBrowser = typeof window !== 'undefined';

  if (isBrowser && typeof navigator !== 'undefined') {
    var tabletRegExp = /(ipad|tablet|(android(?!.*mobile))|(windows(?!.*phone)(.*touch))|kindle|playbook|silk|(puffin(?!.*(IP|AP|WP))))/;
    var phoneRegExp = /(mobi|ipod|phone|blackberry|opera mini|fennec|minimo|symbian|psp|nintendo ds|archos|skyfire|puffin|blazer|bolt|gobrowser|iris|maemo|semc|teashark|uzard)/;
    var userAgentString = navigator.userAgent.toLowerCase();
    return userAgentString.match(tabletRegExp) ? 'tablet' : userAgentString.match(phoneRegExp) && 'mobile' || 'desktop';
  }

  return undefined;
}

function getDeviceType() {
  if (!memo) {
    memo = calc();
  }

  return memo;
}