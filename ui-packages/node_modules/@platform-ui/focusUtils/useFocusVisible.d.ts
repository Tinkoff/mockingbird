import type { FocusEventHandler, MouseEventHandler } from 'react';
interface FocusVisibleProps<TElement extends HTMLElement> {
    focused: boolean;
    onFocus: FocusEventHandler<TElement>;
    onBlur: FocusEventHandler<TElement>;
    onClick?: MouseEventHandler<TElement>;
    onMouseDown?: MouseEventHandler<TElement>;
}
export declare const useFocusVisible: <TElement extends HTMLElement, TProps extends FocusVisibleProps<TElement>>(props: TProps) => TProps & {
    focusVisible: boolean;
};
export {};
