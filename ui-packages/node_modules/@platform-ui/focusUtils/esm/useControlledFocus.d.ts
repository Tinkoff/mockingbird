import type { Ref, FocusEventHandler } from 'react';
interface ControlledFocusProps<TElement extends HTMLElement> {
    ref?: Ref<TElement>;
    focused: boolean;
    onFocus: FocusEventHandler<TElement>;
    onBlur: FocusEventHandler<TElement>;
}
export declare const useControlledFocus: <TElement extends HTMLElement, TProps extends ControlledFocusProps<TElement>>(props: TProps) => TProps & {
    ref: Ref<TElement>;
};
export {};
