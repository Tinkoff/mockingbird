"use strict";

exports.__esModule = true;
exports.updateMultiselectValue = exports.getOptionsLength = void 0;

/**
 * Возвращает новое значение мультиселекта на основе текущего и новой выбранной опции
 */
var updateMultiselectValue = function updateMultiselectValue(value, optionValue) {
  var index = value.indexOf(optionValue);

  if (index === -1) {
    return [].concat(value, [optionValue]);
  }

  var result = [].concat(value);
  result.splice(index, 1);
  return result;
};

exports.updateMultiselectValue = updateMultiselectValue;

var getOptionsLength = function getOptionsLength(options) {
  return options && options.reduce(function (result, option) {
    return result + (option.options ? getOptionsLength(option.options) : 1);
  }, 0);
};

exports.getOptionsLength = getOptionsLength;