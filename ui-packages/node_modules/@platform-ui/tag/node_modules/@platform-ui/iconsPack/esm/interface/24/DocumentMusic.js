import { getIconClass } from '@platform-ui/icon';
import icon from './DocumentMusic.svg';
export default getIconClass(icon, {
  iconSize: 24,
  theme: {
    color: 'rgba(143, 117, 209, 1)',
    background: 'rgba(143, 117, 209, .15)'
  }
});