"use strict";

exports.__esModule = true;
exports.getDirectionsQueue = getDirectionsQueue;
var directionsQueue = {
  top: ['top', 'bottom', 'right', 'left'],
  left: ['left', 'right', 'top', 'bottom'],
  right: ['right', 'left', 'top', 'bottom'],
  bottom: ['bottom', 'top', 'right', 'left']
};

function getDirectionsQueue(direction) {
  return Array.isArray(direction) ? direction : directionsQueue[direction];
}