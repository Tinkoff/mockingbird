"use strict";

exports.__esModule = true;
exports.getViewportRect = getViewportRect;

var _findOverflowParent = require("./findOverflowParent");

var _intersectRects = require("./intersectRects");

var _getWindowRect = require("./getWindowRect");

function getViewportRect(elem) {
  var windowRect = (0, _getWindowRect.getWindowRect)();
  var overflowParent = (0, _findOverflowParent.findOverflowParent)(elem);

  if (!overflowParent) {
    return windowRect;
  }

  return (0, _intersectRects.intersectRects)(windowRect, overflowParent.getBoundingClientRect());
}