import React from 'react';
import type { DataQaInterface } from '@platform-ui/dataQa';
import type { Direction } from './utils/types';
export interface PopoverBlockProps extends DataQaInterface, Omit<React.HTMLProps<HTMLDivElement>, 'className'> {
    animated: boolean;
    popoverBlock: React.ElementType<HTMLDivElement>;
    direction: Direction;
    increasedTouchZone: boolean;
    refCallback: React.RefCallback<React.ElementRef<'div'>>;
    opened: boolean;
    animationDisabled: boolean;
}
declare const PopoverBlock: (props: PopoverBlockProps) => JSX.Element | null;
export default PopoverBlock;
