export function getWindowRect() {
  const width = window.innerWidth;
  const height = window.innerHeight;
  return {
    width,
    height,
    top: 0,
    left: 0,
    right: width,
    bottom: height
  };
}