import { findOverflowParent } from './findOverflowParent';
import { intersectRects } from './intersectRects';
import { getWindowRect } from './getWindowRect';
export function getViewportRect(elem) {
  const windowRect = getWindowRect();
  const overflowParent = findOverflowParent(elem);

  if (!overflowParent) {
    return windowRect;
  }

  return intersectRects(windowRect, overflowParent.getBoundingClientRect());
}