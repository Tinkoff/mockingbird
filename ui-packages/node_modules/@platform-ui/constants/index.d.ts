import iconDesktopSizes from './iconDesktopSizes';
import iconMobileSizes from './iconMobileSizes';
import border from './border';
import layers from './layers';
import media from './media';
import shadow from './shadow';
import spacing from './spacing';
import { SPACING_ALIAS, SHADOW_ALIAS, MEDIA_ALIAS, LAYERS_ALIAS, BORDER_ALIAS } from './alias';

export {
    iconDesktopSizes,
    iconMobileSizes,
    border,
    BORDER_ALIAS,
    layers,
    LAYERS_ALIAS,
    media,
    MEDIA_ALIAS,
    shadow,
    SHADOW_ALIAS,
    spacing,
    SPACING_ALIAS
};
