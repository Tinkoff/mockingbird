"use strict";

exports.__esModule = true;
exports.isObjectTag = isObjectTag;
exports.isTagGroup = isTagGroup;

function isObjectTag(tag) {
  return !!(tag && tag.title);
}

function isTagGroup(option) {
  return !!(option && option.options);
}