import type { Option, AutocompleteContextType } from './types';
declare function useSuggestState(params: {
    focused: boolean;
    disabled: boolean;
    query: string;
    request: (query: string) => Option[] | Promise<Option[]>;
}): AutocompleteContextType;
export default useSuggestState;
