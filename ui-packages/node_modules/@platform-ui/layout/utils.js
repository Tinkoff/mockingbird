"use strict";

exports.__esModule = true;
exports.getSpacing = void 0;

var _constants = require("@platform-ui/constants");

var getSpacing = function getSpacing(value) {
  if (!value) return;

  if (typeof value === 'number') {
    return value + "px";
  }

  return value.split(' ').reduce(function (accum, valueItem) {
    var spacingItem = valueItem in _constants.SPACING_ALIAS ? _constants.spacing[_constants.SPACING_ALIAS[valueItem]] : valueItem;
    return accum + " " + spacingItem;
  }, '').trim();
};

exports.getSpacing = getSpacing;