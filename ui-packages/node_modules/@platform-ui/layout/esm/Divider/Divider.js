import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
const _excluded = ["color", "vertical"];
import React, { forwardRef } from 'react';
import Box from '../Box/Box';
import styles from './Divider.module.css';
const Divider = /*#__PURE__*/forwardRef((_ref, ref) => {
  let {
    color,
    vertical
  } = _ref,
      props = _objectWithoutPropertiesLoose(_ref, _excluded);

  const width = vertical ? '1px' : '100%';
  const height = vertical ? undefined : '1px';

  const style = _extends({
    backgroundColor: color
  }, vertical && {
    minHeight: '100%',
    alignSelf: 'stretch'
  });

  return /*#__PURE__*/React.createElement(Box, Object.assign({
    ref: ref,
    width: width,
    height: height,
    className: styles.root,
    style: style
  }, props));
});
export default Divider;