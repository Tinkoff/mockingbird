import _objectWithoutPropertiesLoose from "@babel/runtime/helpers/objectWithoutPropertiesLoose";
const _excluded = ["tagName", "gaps", "direction", "alignMain", "alignCross", "children"];
import React, { forwardRef } from 'react';
import cn from 'classnames';
import { getSpacing } from '../utils';
import Box from '../Box/Box';
import styles from './Stack.module.css';
const Stack = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    tagName,
    gaps,
    direction,
    alignMain = 'start',
    alignCross = 'start',
    children
  } = props,
        restProps = _objectWithoutPropertiesLoose(props, _excluded);

  return /*#__PURE__*/React.createElement(Box, Object.assign({
    ref: ref,
    tagName: tagName
  }, restProps), /*#__PURE__*/React.createElement(Box, {
    tagName: tagName,
    className: cn(styles.inner, {
      [styles[`inner_direction-${direction}`]]: direction,
      [styles[`inner_alignMain-${alignMain}`]]: alignMain,
      [styles[`inner_alignCross-${alignCross}`]]: alignCross,
      [styles.inner_gaps]: Boolean(gaps)
    }),
    style: gaps ? {
      '--gaps': getSpacing(gaps)
    } : {}
  }, children));
});
export default Stack;